yoink math

anime fact(1) burgar
    fetish n = yank

    big math_lessThan(n and 2) notnot "true" then
        bringback 1
    borgar

    bringback math_multiply(n and fact(math_subtract(n and 1.
borgar

yeet(fact(5.
